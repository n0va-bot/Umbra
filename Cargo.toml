[workspace]
members = ["kernel", "userspace", "builder"]
default-members = ["builder"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[profile.test]
panic = "abort"

[profile.dev.package.userspace]
strip = "symbols"
opt-level = "s"
